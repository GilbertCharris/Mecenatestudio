MECENATE v3.8 — STABLE ROLLBACK

Questa è una versione di emergenza pulita.

RIPRISTINA:
- struttura stabile della home;
- tre filtri analogici dei servizi;
- pagina Film con frame scroll;
- pagine Digital e Direction;
- favicon;
- ritratto originale su fondo bianco;
- footer stabile.

RIMUOVE TEMPORANEAMENTE:
- sfondo fotografico sfocato del ritratto;
- sovrascritture CSS accumulate;
- selettore lingua sperimentale;
- contatti mascherati sperimentali;
- full-bleed sperimentale che causava errori.

INSTALLAZIONE
1. Sostituisci TUTTI i file nella cartella:
   ~/Documents/mecenate-studio-v2

2. Pubblica:
   cd ~/Documents/mecenate-studio-v2
   git add .
   git commit -m "Restore stable Mecenate version"
   git push

Dopo il ripristino, le funzioni lingua e contatti verranno reinserite una alla volta,
senza aggiungere altri blocchi CSS incompatibili.
